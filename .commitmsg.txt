fix(addon+runtime): facing is DETECTION-ONLY - never gate the cast on our own facing read; the client is the sole determinate authority

User directive (absolute, repeated): no single ability may lock up the rotation; facing is detection-only; nothing may be a guess or undetermined.

00:11 log: wait facing:Blood Strike x80 at edge=0yd and RANGED Icy Touch at edge=0yd - while SafeNativeCast returned al=1 (the client ACCEPTED those casts). The developer-facing facing read [obj+0x7AC] intermittently returns 0.0 (FacingLive face=0.0000 nearly every line), so IsFacing compared the target against north and reported a confident not-facing. That false pre-block WAS the entire lockup - the client would have accepted the cast.

DETERMINISTIC FIX (no guess either way):
1. Executor.lua: removed the if-facing-false-then-last_why-facing hard block from BOTH the candidate gate and the target-relative wire path. The cast ALWAYS wires; the client is the referee - al=1 accepted or al=0 refused (one phantom, recovered, logged). A determinate outcome every time, NEVER a multi-second stall. Kept only the LoS confident-block.
2. ObjectManager.cpp: LooksLikeFacing* reject abs(f)<0.0001 (the 0.0 failed-read sentinel); camera [cam+0x11C] (the client's own GetPlayerFacing fallback, 0x60A4EA) is the primary source for diagnostics/candidate ordering.
3. AuraSearchPacked still orders closest + facing-first, but that never holds the wire.

Refusing to cast on a measurement we cannot trust is itself a wrong, non-deterministic guess. Delegating the facing decision to the client gives a determinate result every time.
