round 51 — runtime-only resource gate (fail-open) + last taint removed (IsUsableSpell/IsSpellInRange)

Fixes the 14:57 session:
  * "nothing is casting other than auto attack consecration" — the round-50 rune
    gate FAILED CLOSED: RuneState read 0:0:0 (regenerating/unknown) and blocked
    EVERY rune spell -> `wait no_rune x18` even with no target.
  * taint STILL present (`bl()` x1) — IsUsableSpell and IsSpellInRange are
    hardware-gated FrameScript APIs; calling them from addon Lua taints even as
    no-ops.

1. World.resource_ok(sid) — the SINGLE runtime-only resource gate (fail-open):
   - Rune spells (PS/BS->blood, IT->frost): RuneState ("blood:frost:unholy");
     block ONLY with positive evidence (total>0 AND required-type count==0);
     all-zero (regenerating/unreadable) = UNKNOWN = ALLOW (client is final
     referee; genuine refusals bounded by the 0.6s resource floor).
   - Mana spells (Renew/Flash Heal/Consecration/Lesser/Greater Heal): runtime
     UnitPower(0)/UnitMaxPower(0) (GuidArg 0 = local player); block only if
     max>0 and cur==0.
   - Unknown spell: allow.
2. ALL IsUsableSpell (5 sites) + IsSpellInRange (2 sites) calls removed from
   BasicRules.check_resources, Executor (pre-wire, fill_live_spell_state,
   spell_in_range_vs_target) and World (spell_usable fill, range snapshot) —
   all route through World.resource_ok / the runtime range model. Runtime
   ValidateCast stays DISABLED (CRASH RULE); GetSpellCooldown (not gated) stays
   Lua-side.
3. RUNTIME: RuneStatePacked treats a GetRuneType failure as type 3 (counts for
   all types) so it never falsely reports a required type as 0. Version
   1.10.107-aa -> 1.10.108-aa.
4. tools/_verify_round47.py: PROOF 6 now bans IsUsableSpell/IsSpellInRange
   (147 deployed .lua, zero protected calls); PROOF 8 asserts the fail-open
   rune gate (all-zero ALLOWS, positive-evidence BLOCKS) + deployed
   World.resource_ok. 8 proofs exit 0.
5. PENDING_LIVE_VERIFICATION.md round 51 section added.

Live check after re-inject 1.10.108-aa + /reload: rotation casts normally (no
all-no_rune lockup); ZERO red notifications possible (no protected call remains
in addon Lua); a genuine no-rune attempt = one refusal + the 0.6s resource
floor, never a storm.
